#include "headerfile.h"

typedef struct
{

} s_solution;

int     ft_rec_search_optimal_solution(s_solution sol)
{
    int     i;
    int     direction;

    i = 0;
    direction = 0;
    while (i < n_tetra)
    {
        while (direction < 2)
        {
            sol[][][]
            ft_rec_search_optimal_solution(s_solution sol);
            direction++;
        }
        direction = 0;
        i++;
    }
}    